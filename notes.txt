August 19, 2021

# From Rick and others
sudo apt-get update
sudo apt-get upgrade
sudo rpi-update
sudo apt install raspberrypi-kernel-headers git bc bison flex libssl-dev make

cd /opt/git/RPDriver
make
sudo insmod hello.ko
sudo rmmod hello.ko

### Old way that worked with: ###
#  Makefile
   obj-m := hello.o
# hello.c and Makefile are from: https://raspberrypi.stackexchange.com/questions/39845/how-compile-a-loadable-kernel-module-without-recompiling-kernel
make -C /lib/modules/$(uname -r)/build M=$(pwd) modules
sudo insmod hello.ko
sudo rmmod hello.ko
